tools:
  ffprobe:
    path: ../tools/ffmpeg/bin/ffprobe.exe

  ffmpeg:
    path: ../tools/ffmpeg/bin/ffmpeg.exe

  virtualdub:
    executable: ../tools/VirtualDub2/vdub64.exe
    path_mode: windows
    path_mappings: []
    env: {}

paths:
  workflows: workflows
  avisynth: ../tools/AviSynth+/plugins64+

processing:
  output_dir: ../output
  work_dir: ../work
  keep_temp_files: true
